return {
  "at-popup",
  dir = "~/Projects/lua-tutorials/at-popup",
  config = function()
    require("at-popup").setup({
      -- message = "Hello there, this is @ symbol being tested",
      -- delay = 10, -- ms delay before showing popup
    })
  end,
}
